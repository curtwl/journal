{
  "name": "journal",
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "concurrently \"npm start --prefix backend\" \"cross-env PORT=3000 npm start --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^6.0.0",
    "cross-env": "^7.0.3"
  }
}
  
